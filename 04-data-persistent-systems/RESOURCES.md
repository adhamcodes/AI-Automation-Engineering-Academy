# Resources

Primary: PostgreSQL current tutorial for SQL/transactions and academy exercises for state/idempotency. Use n8n database/infrastructure docs only as implementation references. Queue products are optional; first learn the semantics with a simple worker model.
