# Phase 4 Assessment — Persistent Automation

Build a database-backed automation from an unfamiliar event specification. The evaluator repeats events, crashes a worker after an external side effect, and runs two workers concurrently. Pass only if state remains correct, duplicate side effects are prevented, recovery is documented, and schema/transaction choices are explained.
