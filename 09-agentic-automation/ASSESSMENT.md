# Phase 9 Assessment — Bounded Agent

Build a tool-using agent as one component of a deterministic workflow. It must have explicit state, least-privilege tools, max-step/cost limits, approval for one consequential action, traceable errors and a trajectory evaluation set. Hidden tests include prompt injection and tool outage.
