# AI Evaluation Lab

This lab teaches evaluation before provider choice.

`cases.json` contains deterministic expected behavior. `evaluator.py` scores exact structured outputs from a mock automation classifier.

Extend the dataset to at least 30 cases covering happy paths, ambiguity, unsafe requests, malformed input, and escalation. Then compare two prompt/model implementations using the same frozen cases.
