# JavaScript Async Lab

Write a small Node script that receives three independent mock API tasks, runs them concurrently with bounded concurrency, preserves input/output correlation, and reports individual failures without hiding successful results.

Then modify one task to reject and explain the difference between fail-fast aggregation and collecting settled results.
