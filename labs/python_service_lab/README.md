# Python Service Lab

`service.py` contains deterministic normalization logic plus a minimal local HTTP endpoint using only the standard library.

Your job is to wrap similar logic in the framework taught by the phase, add validation, structured errors, tests, configuration, and an n8n-to-Python call.

The included test proves the reference normalization contract, not your complete service design.
