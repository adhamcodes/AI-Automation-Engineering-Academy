# Phase 5 Assessment

Given nested webhook data and an unfamiliar API, transform/validate items, perform asynchronous calls with explicit failure handling, and return a clean output contract suitable for n8n. Explain where native nodes would be clearer than code.
