# Module 05.5 — HTTP with fetch

## Capability
Make authenticated JS API requests with status/error handling.

## Core model
fetch resolves on HTTP errors too; inspect status, parse cautiously, handle timeouts/abort where supported.

## Practice
Implement GET/POST with auth and validation.

## Failure / debugging task
Repair code that assumes every response is JSON/200.

## Evidence to save
Reusable request wrapper.

## Mastery
API failure becomes explicit structured error.
